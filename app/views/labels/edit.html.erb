</br></br>

    <% if @label.errors.any? %>
            <div class="btn btn-danger" id="error_explanation"> 

                <ul>
                    <% @label.errors.full_messages.each do |message|  %>
                        <%if message.include?("Name") %>
                            <% message = "名前を入力してください" %>
                        <% end %>
                    <li><%= message %></li>
                    <% end %>
                </ul>
            </div>
    <% end %>
    
    <div class="card-header">   
        <h1>ラベル登録ページ</h1>
    </div>
  
  <div class="card-body ">
        <div class="form-group ">
            <%= form_for @label do |f| %>
                <%= f.label "名前" , class: "control-label"%>
                <div>
                    <%= f.text_field :name, class: "form-control " %>
                </div>
              
                </br>
                <div>
                    <button type="submit" class="btn btn-success" >更新する</button></br>
                    <a class="btn btn-link " href="<%= labels_path %>" id="back">戻る</a>
                </div>
                
            <% end %>
        
        </div>
    </div>
    
    