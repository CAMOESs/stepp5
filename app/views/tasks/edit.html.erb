</br></br>

<div class="card ">
<% if @task.errors.any? %>
    <div class="btn btn-danger"> 
        <ul>
            <% @task.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
        </ul>
    </div>
<% end %>

  <div class="card-header">
    Edit task page
  </div>
  <div class="card-body">
        <div class="form-group">
            <%= form_for @task do |f| %>
                <%= f.label :title,"The title" , class: "control-label"%>
                <div class="">
                    <%= f.text_field :title, class: "form-control " %>
                </div>
                <%= f.label :content,"Contents" , class: " control-label"%>
                <div class="">
                    <%= f.text_field :content, class: "form-control " %>
                </div>
                </br>
                <div>
                    <button type="submit" class="btn btn-primary" id="create-task">Update Task</button></br>
                    <a class="btn btn-link " href="<%= root_path %>" id="return">Back</a>
                </div>
            <% end %>
        
        </div>
    </div>
    
    
   
</div>
