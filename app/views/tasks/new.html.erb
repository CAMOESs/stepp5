</br></br>

<div class="card ">
<% if @task.errors.any? %>
    <div class="btn btn-danger" id="error_explanation"> 
    <h2><%= pluralize(@task.errors.count, "error") %> prohibited this <%= @task.title %> from being saved:</h2>

        <ul>
            <% @task.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    </div>
<% end %>

  <div class="card-header">
    New task page
  </div>
  <div class="card-body">
        <div class="form-group">
            <%= form_for @task do |f| %>
                <%= f.label :title,"The title" , class: "control-label"%>
                <div class="">
                    <%= f.text_field :title, class: "form-control " %>
                </div>
                <%= f.label :content,"Contents" , class: " control-label"%>
                <div class="">
                    <%= f.text_field :content, class: "form-control " %>
                </div>
                </br>
                <div>
                    <button type="submit" class="btn btn-success" id="update-user">Create Task</button></br>
                    <a class="btn btn-link " href="<%= root_path %>" id="return">Back</a>
                </div>
            <% end %>
        
        </div>
    </div>
    
    
   
</div>
